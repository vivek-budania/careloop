# Get your free API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_api_key_here

# Optional. Mock coverage works without this. A Stedi *test* key can probe 270/271;
# fixture member IDs on main will not match sandbox canned members, so eligibility
# still comes from CareLoop mocks until the Jane Doe / Aetna adapter lands.
#
# Do not put the real value here or in git. For Vercel: Project Settings →
# Environment Variables → name STEDI_API_KEY (Production + Preview) → Redeploy.
# Cursor / cloud-agent env does not reach Vercel. Production Stedi keys are out of scope.
# STEDI_API_KEY=